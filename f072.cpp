#include <iostream>
#include <vector>
using namespace std;

int main(){
    int N;
    cin >> N;
    vector<int> space;
    for(int i = 0; i<N; i++){
        int tmp;
        cin >> tmp;
        space.push_back(tmp);
    }

}