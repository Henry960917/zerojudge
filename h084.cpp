#include <iostream>
#include <vector>
using namespace std;

int main(){
    int n,k;
    cin >> n >> k;
    vector<int> h(n);
    vector<int> w(k);
    for(int i = 0; i<n; i++){
        cin >> h[i];
    }
    for(int i = 0; i<k; i++){
        cin >> w[i];
    }
    
}